export type CivilStatus = {
  id: string;
  codigo: string;
  name: string;
};

export type ColorRace = {
  id: string;
  codigo: string;
  name: string;
};

export type Formations = {
  id: string;
  codigo: string;
  name: string;
};

export type Kinships = {
  id: string;
  codigo: string;
  name: string;
};

export type MemberSituations = {
  id: string;
  codigo: string;
  name: string;
};
